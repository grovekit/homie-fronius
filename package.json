{
  "name": "@grovekit/homie-fronius",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "node --test dist/**/*.test.js",
    "ts:clean": "rm -rf ./dist && rm -f ./tsconfig.tsbuildinfo",
    "ts:build": "npm run ts:clean && tsc",
    "ts:watch": "npm run ts:clean && tsc --watch",
    "build": "npm run ts:build",
    "postinstall": "npx runtyped-install-transformer"
  },
  "author": "Jacopo Scazzosi <jacopo@scazzosi.com>",
  "license": "MIT",
  "homepage": "https://github.com/grovekit/homie-fronius",
  "devDependencies": {
    "@runtyped/type-compiler": "^2.0.0-alpha.3",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@grovekit/homie-client": "^0.0.1-alpha.11",
    "@grovekit/homie-core": "^0.0.1-alpha.4",
    "@runtyped/type": "^2.0.0-alpha.3",
    "loopyloop": "^4.0.2",
    "pinetto": "^4.0.0"
  }
}
